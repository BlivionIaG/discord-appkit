# User token of the Discord account that owns the applications.
# Set this only on a protected GitHub Environment named discord-portal.
# Never commit a real value. Catalog sync does not need it.
DISCORD_USER_TOKEN=

# Optional API base. Defaults to https://discord.com/api/v10
DISCORD_API_BASE=https://discord.com/api/v10

