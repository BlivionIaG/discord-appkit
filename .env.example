# User token of the Discord account that owns the applications.
# Bot tokens cannot create apps or manage RPC assets.
# Never commit a real value. Never put this in a public repository.
DISCORD_USER_TOKEN=

# Optional API base. Defaults to https://discord.com/api/v10
DISCORD_API_BASE=https://discord.com/api/v10

# GitHub token used only by the private catalog-sync workflow to open a PR
# on the public FetchCord repo. This is not a Discord token.
# FETCHCORD_SYNC_TOKEN=
